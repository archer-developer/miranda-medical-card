.env
.idea
data/
logs/
dist/
go.work
go.work.sum

# Real config files are environment-specific and may carry deployment
# details; only the documented, generic config.yaml.dist template is
# checked in. This pattern only matches "*.yaml" exactly, so it does not
# ignore config.yaml.dist itself.
config/*.yaml
